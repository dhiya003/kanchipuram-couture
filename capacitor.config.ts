import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sareeheritage.app',
  appName: 'Kanchipuram Couture',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
